import { InjectionToken } from '@angular/core';
import { GetVehicleGateway } from './get-vehicle-gateway';

export const GET_VEHICLE_GATEWAY = new InjectionToken<GetVehicleGateway>(
  'GET_VEHICLE_GATEWAY'
);
